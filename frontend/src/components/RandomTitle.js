function RandomTitle({ word }) {
  // eslint-disable-next-line react/no-unescaped-entities
  return <h1 className="textshadow textborder text-8xl text-primary-1 font-cookierun">{`'${word}' 를 그려보세요!`}</h1>;
}
export default RandomTitle;
